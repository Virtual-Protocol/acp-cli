# ACP API server URL (required for `acp configure`)
ACP_API_URL=

# Chain ID (default: 84532 — Base Sepolia)
# ACP_CHAIN_ID=84532

# Privy App ID (enables automatic signer flow on `acp agent create`)
# ACP_PRIVY_APP_ID=
